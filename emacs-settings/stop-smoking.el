



(defun calc-smoke-time (start-time interval)

  (let ( (st  (parse-time-string  start-time) )
         (si (parse-time-string  interval)))
  ;; (message st)
    )
)
(display-time)

(calc-smoke-time "8:00" "0:70")

